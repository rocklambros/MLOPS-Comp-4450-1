# Dev/test dependencies for hw7: both service runtimes plus the test and eval tooling.
# `make test` imports main (api runtime) and reference_stats (monitoring runtime), and
# evaluate.py needs requests. Compile with:
#   uv pip compile requirements-dev.in --generate-hashes --universal --python-version 3.13 -o requirements-dev.txt
-r api/requirements.in
-r monitoring/requirements.in
pytest==9.1.0
httpx==0.28.1
