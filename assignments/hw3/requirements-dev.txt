# Dev/test dependencies, kept out of the runtime image (see .dockerignore).
# Install on top of the runtime set: pip install -r requirements-dev.txt
-r requirements.txt
pytest==9.1.0
httpx==0.28.1
ruff==0.15.17
